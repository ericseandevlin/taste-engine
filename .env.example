# Copy to .env and fill in. The .env file is gitignored.
#
# Required: an Anthropic API key. The tag/cluster/directions/thesis/generate/critique
# stages call the Claude API. The import/measure/atlas stages are local-only
# and need no key.
#
# Get a key: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-...
